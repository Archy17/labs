/*

  Array.unshift( valores )

  - Insere argumentos no início do array
  ps: retorna o tamanho do array

*/

var numbers = [ 4, 5, 6 ];

numbers.unshift( 3 );
console.log( numbers );

console.log( numbers.unshift( 2 ) );
console.log( numbers );


console.log( numbers.unshift( 1 ) );
console.log( numbers );
