/*

  Array.pop()

  - Exclui o último elemento do array
  - Retorna o valor excluído
  ps: Se o array está vazio, não altera nada e retorna undefined

*/

var stack = [ 'JS', 'HTML', 'CSS' ];

console.log( stack.pop() );
console.log( stack );

console.log( stack.pop() );
console.log( stack );

console.log( stack.pop() );
console.log( stack );

console.log( stack.pop() );
console.log( stack );

