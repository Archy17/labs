// Math.min() : retorna o menor argumento

console.log( 'Args: 1, 4, 6, 8, 13, 2 => ' + Math.min( 1, 4, 6, 8, 13, 2 ) );
console.log( 'Args: [ 1, 4, 6, 8, 13, 2 ] => ' + Math.min( [ 1, 4, 6, 8, 13, 2 ] ) );

/*
  Retorna o menor dos argumentos. Caso o argumento não possa ser convertido
  em um valor númerico, retorna NaN
*/
